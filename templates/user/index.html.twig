{% extends 'admin.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>Liste des producteurs</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <!-- <th>Password</th> -->
                <!-- <th>Token</th> -->
                <th>Nom</th>
                <th>Prénom</th>
                <th>Siret</th>
                <th>Tél</th>
                <th>Adresse</th>
                <th>CP</th>
                <th>Ville</th>
                <th>Horaires</th>
                <!-- <th>Categories</th> -->
                <th>Site</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <!-- <td>{{ user.password }}</td> -->
                <!-- <td>{{ user.token }}</td> -->
                <td>{{ user.lastname }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.siret }}</td>
                <td>{{ user.phone }}</td>
                <td>{{ user.adress }}</td>
                <td>{{ user.cp }}</td>
                <td>{{ user.ville }}</td>
                <td>{{ user.horaire }}</td>
                <!-- <td>{{ user.category }}</td> -->
                <td>{{ user.site }}</td>
                <td>
                    <a href="{{ path('user_show', {'id': user.id}) }}"><i class="fas fa-eye" title="afficher"></i></a>
                    <a href="{{ path('user_edit', {'id': user.id}) }}"><i class="fas fa-user-edit" title="éditer"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="14">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ paginator|raw }}

    <!-- <a href="{{ path('user_new') }}">Create new</a> -->
{% endblock %}
